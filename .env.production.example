# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES FOR NETLIFY DEPLOYMENT
# =============================================================================
# 
# IMPORTANT: 
# 1. Copy this file to .env.local for local development
# 2. Set these variables in Netlify Dashboard > Site Settings > Environment Variables
# 3. NEVER commit actual secrets to version control
#
# =============================================================================

# Firebase Admin SDK Configuration (REQUIRED)
# Get these from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_ACTUAL_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Firebase Client Configuration (for frontend)
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-firebase-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef123456789

# Build Configuration
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://your-site.netlify.app
NEXT_PUBLIC_API_URL=https://your-site.netlify.app/api

# =============================================================================
# NETLIFY DEPLOYMENT INSTRUCTIONS:
# =============================================================================
# 
# 1. Go to Netlify Dashboard > Your Site > Site Settings > Environment Variables
# 2. Add each variable above with your actual values
# 3. For FIREBASE_PRIVATE_KEY: 
#    - Copy the entire private key including headers/footers
#    - Ensure newlines are preserved (use quotes)
#    - The key should start with -----BEGIN PRIVATE KEY-----
#    - The key should end with -----END PRIVATE KEY-----
# 4. Deploy your site
#
# =============================================================================

# Optional: Additional API Keys (if needed)
# OPENAI_API_KEY=your-openai-api-key
# STRIPE_SECRET_KEY=your-stripe-secret-key
# STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret
